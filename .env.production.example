# ==========================================
# CONFIGURAÇÃO DE PRODUÇÃO - VIP ASSIST
# ==========================================

# IMPORTANTE: Copie este arquivo para .env.production e preencha com valores reais
# NUNCA commite o arquivo .env.production no Git!

# ==========================================
# DATABASE
# ==========================================
# PostgreSQL em Docker (VPS)
# ✅ SEGURANÇA: Use senha forte (mínimo 32 caracteres, letras, números e símbolos)
# ✅ SEGURANÇA: Porta 5432 NÃO está exposta publicamente no docker-compose.prod.yml
# Para acesso externo, use SSH tunnel: ssh -L 5432:localhost:5432 user@server
DATABASE_URL="postgresql://vipassist:SENHA_FORTE_AQUI@localhost:5432/vipassist?schema=public"

# Variáveis para Docker Compose
POSTGRES_USER=vipassist
# ⚠️ CRÍTICO: Altere esta senha! Use: openssl rand -base64 32
POSTGRES_PASSWORD=SENHA_FORTE_AQUI
POSTGRES_DB=vipassist

# ==========================================
# NEXT.JS
# ==========================================
NODE_ENV=production
NEXTAUTH_SECRET=gere-um-secret-de-32-caracteres-aleatorios-aqui
NEXTAUTH_URL=https://seu-dominio.com

# ==========================================
# GOOGLE MAPS API
# ==========================================
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=sua-chave-google-maps-aqui

# ==========================================
# MAPBOX API
# ==========================================
NEXT_PUBLIC_MAPBOX_TOKEN=seu-token-mapbox-aqui

# ==========================================
# BACKUP
# ==========================================
BACKUP_KEEP_DAYS=7
BACKUP_KEEP_WEEKS=4
BACKUP_KEEP_MONTHS=6

# ⚠️ CRÍTICO: Chave de criptografia para backups (AES-256-CBC)
# Gere uma chave forte: openssl rand -base64 32
# NUNCA perca esta chave! Sem ela, os backups não podem ser restaurados!
# Armazene em local seguro (gerenciador de senhas, cofre, etc.)
BACKUP_ENCRYPTION_KEY=gere-uma-chave-de-32-caracteres-aqui

# ==========================================
# OBSERVAÇÕES DE SEGURANÇA
# ==========================================
# 1. Gere senhas fortes: openssl rand -base64 32
# 2. Gere NEXTAUTH_SECRET: openssl rand -base64 32
# 3. ✅ Porta PostgreSQL NÃO exposta publicamente (docker-compose.prod.yml)
# 4. Para acesso ao banco: use SSH tunnel (ssh -L 5432:localhost:5432 user@server)
# 5. Configure firewall (ufw) para bloquear porta 5432 externamente
# 6. Use SSL/TLS em produção (configure no Nginx/Caddy)
# 7. Backups automáticos configurados no docker-compose.prod.yml
# 8. ✅ Migrations do Prisma versionadas no Git (prisma/migrations/)
